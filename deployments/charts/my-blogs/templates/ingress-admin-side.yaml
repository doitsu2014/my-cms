{{- if and .Values.adminSide.enabled .Values.adminSide.ingress.enabled -}}
{{- $fullName := include "my-blogs.adminSide.fullname" . -}}
{{- $svcPort := .Values.adminSide.service.port -}}
{{- if and .Values.adminSide.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.adminSide.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.adminSide.ingress.annotations "kubernetes.io/ingress.class" .Values.adminSide.ingress.className}}
  {{- end }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-ingress
  labels:
    {{- include "my-blogs.adminSide.labels" . | nindent 4 }}
  {{- with .Values.adminSide.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.adminSide.ingress.className }}
  ingressClassName: {{ .Values.adminSide.ingress.className }}
  {{- end }}
  {{- if .Values.adminSide.ingress.tls }}
  tls:
    {{- range .Values.adminSide.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.adminSide.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType | default "Prefix" }}
            backend:
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
