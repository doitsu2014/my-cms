{{- if and .Values.clientSide.enabled .Values.clientSide.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "my-blogs.clientSide.fullname" . }}
  labels:
    {{- include "my-blogs.clientSide.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "my-blogs.clientSide.fullname" . }}
  minReplicas: {{ .Values.clientSide.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.clientSide.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.clientSide.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.clientSide.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.clientSide.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.clientSide.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
