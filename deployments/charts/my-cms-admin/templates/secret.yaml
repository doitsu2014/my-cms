{{- if or .Values.adminSide.enabled .Values.clientSide.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "my-cms-admin.fullname" . }}-image-pull-secrets
  labels:
    helm.sh/chart: {{ include "my-cms-admin.chart" . }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ template "my-cms-admin.imagePullSecret" . }}
{{- end }}
