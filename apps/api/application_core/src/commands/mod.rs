// Commands module — extracted into dedicated domain crates (domain_media,
// domain_posts, domain_user). This module remains as an empty stub so the
// `commands` path remains resolvable for any historical doc references.
