//! `CategoryCreateHandler` — application-layer command handler for creating a
//! category with tag attach and translation rows.
//!
//! Moved from `application_core::commands::category::create` per the
//! `consolidate-category-ai-translate-into-domain-posts` change.

pub mod create_handler;
pub mod create_request;
