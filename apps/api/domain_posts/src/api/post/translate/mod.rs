//! Post translation HTTP adapters — `POST /posts/{post_id}/translate`,
//! `POST /posts/{post_id}/translate/background`, and the translation-job
//! endpoints.

pub mod job_handler;
pub mod translate_handler;
