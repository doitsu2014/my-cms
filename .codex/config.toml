# Project-scoped Codex configuration.
# The server is also available globally in the developer's Codex config, but
# keeping it here makes the agent team portable for trusted checkouts.

[mcp_servers.code-review-graph]
command = "uvx"
args = ["code-review-graph", "serve"]
type = "stdio"

[agents]
# Allow up to four threads for independent, read-only exploration and analysis.
# Shared artifact, configuration, and source edits remain serialized.
max_concurrent_threads_per_session = 4
