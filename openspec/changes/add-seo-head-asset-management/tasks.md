## 1. Establish the SEO domain and schema-first persistence

- [ ] 1.1 [seo-head-asset-management] Add `domain_seo` to the API workspace and gateway dependencies, and scaffold its `lib`, `service`, `api`, `domain`, `handlers`, `migrations`, and generated-entity boundaries. Test the `DomainService` health/config/migration/route-registration contract first, then register `DomainSeoService` as the fifth service in `apps/api/gateway/src/main.rs` and ensure the migration CLI uses the unchanged shared manifest. Verify with `cargo test -p domain_seo` and the focused gateway manifest/orchestrator tests.
- [ ] 1.2 [seo-head-asset-management] Write the `domain_seo` migration for `seo_head_assets` with UUID key, unique bounded label, validated source storage, enabled state, positive sort-order check, optimistic row version, timestamps/actor identifiers, and public-order index. Add migration up/down tests using PostgreSQL testcontainers, apply through the gateway migration orchestrator, then generate SeaORM entities into `apps/api/domain_seo/src/entities/` without hand-editing them. Verify with the domain migration test plus `my-cms-api migrate --list` and `my-cms-api migrate up` against the test database.

## 2. Implement and test the trusted head-asset core

- [ ] 2.1 [seo-head-asset-management] Write failing unit tests for a parser-backed source validator in `apps/api/domain_seo/src/handlers/head_assets/`: accept the supplied gtag, JSON-LD, verification meta, and permitted links; reject malformed/oversized source, forbidden tags/attributes, event handlers, typed-metadata overrides, invalid schemes, non-HTTPS URLs, duplicate attributes, and conflicting external/inline script content. Implement the exact allow-list and 32 KiB/field limits without executing, fetching, or logging source. Verify with `cargo test -p domain_seo head_asset_validation`.
- [ ] 2.2 [seo-head-asset-management] Write handler tests before implementing create/list/read/update/delete DTO conversion, SeaORM persistence, deterministic ordering, actor timestamps, unique-label mapping, 404 handling, and conditional `row_version` updates. Require complete replacement requests and map stale updates to the optimistic-concurrency 409 contract. Emit source-free structured audit events for successful mutations. Verify with `cargo test -p domain_seo head_assets` and PostgreSQL testcontainer coverage of constraint and concurrent-update cases.

## 3. Expose API contracts through the gateway

- [ ] 3.1 [seo-head-asset-management] Add thin Axum adapters and `DomainSeoService` route registrations: administrator CRUD at `/seo/head-assets` and `/seo/head-assets/{id}`, plus public `GET /seo/head-assets/ducth-dev`. Use the existing administrator auth mount and response/error envelope; keep source validation/business logic out of adapters. Write router/auth tests first for 401, writer-only 403, admin success, 201/204 success statuses, and public response omission of disabled/audit/version fields. Verify with `cargo test -p domain_seo api` and focused gateway route tests.
- [ ] 3.2 [seo-head-asset-management] Add public-read handler tests covering only enabled records, `sortOrder`/identifier ordering, and empty results. Add startup health and tracing tests that prove source HTML is excluded from audit/error diagnostics. Verify with `cargo test -p domain_seo public_head_assets` and `cargo test -p domain_seo observability`.

## 4. Add the administrator-only CMS experience

- [ ] 4.1 [seo-head-asset-management] Add typed SEO asset models and REST helpers under `apps/web/src/domains/` and the relevant admin feature directory, using `authenticatedFetch(getApiUrl(...))`. Build request/response parsing and React Hook Form + Zod validation for required label, enabled, positive sort order, and source fields; treat server validation/conflict responses as authoritative. Test model/form validation and REST error mapping first with Vitest.
- [ ] 4.2 [seo-head-asset-management] Add responsive list, create, and edit components under `apps/web/src/app/admin/seo/` for `/admin/seo/head-assets` and its create/edit subroutes. Implement loading, empty, retryable read/save failure, accessible labels/errors/focus, keyboard-friendly controls, source textarea as inert data only, stale-update recovery feedback, and confirmed deletion. Add component tests that prove submitted script source is never rendered as executable HTML and deletion is not sent before confirmation. Verify with focused `pnpm --dir apps/web test -- --run` tests for the SEO components.
- [ ] 4.3 [seo-head-asset-management] Wire the pages in `apps/web/src/App.tsx` through `AdminOnlyRoute` and add the SEO navigation entry in `apps/web/src/app/admin/components/left-menu.tsx` only for administrators. Test administrator visibility/route access and writer absence/access-denied behavior, then verify `pnpm --dir apps/web build`.

## 5. Deliver dynamic assets at the Ducth SSR seam

- [ ] 5.1 [seo-head-asset-management] Add the required server-only `WEBSITE_SEO_HEAD_ASSETS_API_URL` to `apps/ducth-dev-website/server.prod.mjs` configuration and `.env.example`, ensuring it is absent from the browser runtime-config payload. Write configuration tests first for missing/invalid endpoint behavior and non-exposure in `app-config`. Verify with focused Vitest production-server/config tests.
- [ ] 5.2 [seo-head-asset-management] Implement an injected/dependency-testable server-only head-asset client/cache module for public envelope validation, 2-second abort timeout, 60-second fresh cache, single-flight refresh, five-minute bounded stale fallback, and source-free refresh/failure telemetry. Write deterministic clock/fetch tests for successful refresh, ordering preservation, timeout/non-200/malformed response, stale expiration, and concurrent request coalescing. Verify with `pnpm --dir apps/ducth-dev-website test -- --run` focused cache tests.
- [ ] 5.3 [seo-head-asset-management] Integrate the client into `apps/ducth-dev-website/server.prod.mjs` so every non-redirect SSR document inserts the validated public HTML once before `</head>` alongside existing metadata and app config, while failures render the existing page normally. Extend `src/test/production-server.test.ts` to prove crawler-visible ordered assets, no disabled asset after successful refresh, typed-metadata/config preservation, and fail-open behavior. Verify with `pnpm --dir apps/ducth-dev-website test -- --run src/test/production-server.test.ts` and `pnpm --dir apps/ducth-dev-website build`.

## 6. Review, integration verification, and release evidence

- [ ] 6.1 [seo-head-asset-management] Run the API/admin/SSR slices together against a PostgreSQL test database: create a disabled asset, confirm public absence, enable the reviewed gtag source, confirm public API and rendered Ducth HTML after refresh, then disable/delete and confirm removal after a healthy refresh. Capture the expected 60-second fresh and five-minute stale bounds without logging the source. Verify the focused testcontainer integration suite and production-server test.
- [ ] 6.2 [seo-head-asset-management] After each implementation task group, run code-review-graph `detect_changes`, `get_affected_flows`, `tests_for` for high-risk API/SSR functions, and `get_impact_radius`; if the graph is still stale/unavailable, record targeted `rg`, `git diff`, and test inspection evidence. Resolve any unexpected caller, auth, migration, or rendering impact before marking the group complete.
- [ ] 6.3 [seo-head-asset-management] Run the repository verification gate and inspect the final diff: `cargo check`, `cargo test`, `cargo fmt -- --check`, `cargo clippy`, `pnpm --dir apps/web build`, and `pnpm --dir apps/ducth-dev-website build`. Record any pre-existing failures separately, verify `openspec validate --change "add-seo-head-asset-management"` (or the repository-supported equivalent), and provide the release handoff with migration order, `WEBSITE_SEO_HEAD_ASSETS_API_URL` rollout, privacy acceptance, cache consistency, and destructive-down-migration warning.
