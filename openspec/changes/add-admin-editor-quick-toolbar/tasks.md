## 1. Capture and apply editor insertion targets

- [x] 1.1 Add RED Vitest coverage in `apps/web/src/app/admin/components/inputs/rich-text-editor/editor-insertion-target.test.ts` for capturing a selection, mapping it through a ProseMirror transaction, inserting consecutive image nodes at the mapped target, and clearing a cancelled/completed target; verify with `pnpm --dir apps/web test -- src/app/admin/components/inputs/rich-text-editor/editor-insertion-target.test.ts`.
- [x] 1.2 Create `apps/web/src/app/admin/components/inputs/rich-text-editor/editor-insertion-target.ts` using the existing `@tiptap/pm` types to own a transaction-mapped selection bookmark and target-relative image, YouTube, and default code-block insertion; do not add a document marker/schema node or a dependency. Make task 1.1 pass.

## 2. Build and test the shared image-insertion dialog

- [x] 2.1 Add RED component coverage in `apps/web/src/app/admin/components/inputs/rich-text-editor/image-insertion-dialog.test.tsx` for authenticated `GET /media`, image-only filtering using returned URLs, visible selection ordinals and confirm order, loading/empty/error-with-retry states, keyboard/cancel focus restoration, and disabled confirmation without a selection; verify with `pnpm --dir apps/web test -- src/app/admin/components/inputs/rich-text-editor/image-insertion-dialog.test.tsx`.
- [x] 2.2 Extend the RED dialog coverage in `apps/web/src/app/admin/components/inputs/rich-text-editor/image-insertion-dialog.test.tsx` for a `multiple` `image/*` file input: sequential existing authenticated upload calls, ordered success insertion, continuation after an individual failed upload, all-failure no insertion, file-input reset, and exact success/failure feedback; verify with the same targeted command.
- [x] 2.3 Implement `apps/web/src/app/admin/components/inputs/rich-text-editor/image-insertion-dialog.tsx` as the single accessible media/library and local-upload surface. Query the existing `getApiUrl('/media')` through `authenticatedFetch`, filter `MediaMetadata` with `isImageContentType`, preserve selection order as a sequence, use returned `url` values, and implement retry/loading/empty/error/cancel/focus states. Upload selected local files sequentially through the existing `getMediaUploadApiUrl()` and `createAuthHeaders(token)`, continue after failures, and submit only ordered successful URLs to its caller. Make tasks 2.1 and 2.2 pass.

## 3. Add the Quick Toolbar and contextual authoring flows

- [x] 3.1 Add RED coverage in `apps/web/src/app/admin/components/inputs/rich-text-editor/quick-toolbar.test.tsx` for a viewport-clamped menu with exactly Add Image, Add Video, and Add Code Block controls; first-action focus; Escape and outside-pointer dismissal; and no mutation on dismissal. Verify with `pnpm --dir apps/web test -- src/app/admin/components/inputs/rich-text-editor/quick-toolbar.test.tsx`.
- [x] 3.2 Implement `apps/web/src/app/admin/components/inputs/rich-text-editor/quick-toolbar.tsx` with only the three labelled action controls, viewport-safe placement, accessible menu semantics, and the close/focus lifecycle specified by task 3.1. Make task 3.1 pass without adding a context-menu dependency.
- [x] 3.3 Add RED integration coverage in `apps/web/src/app/admin/components/inputs/rich-text-editor/tiptap-editor.quick-toolbar.test.tsx` for editable right-click target capture/native-menu suppression; read-only and unresolved-target native behavior; mapped-target image insertion after focus moves or a transaction intervenes; Quick Toolbar YouTube insertion; default code-block insertion; and dialog cancellation. Verify with `pnpm --dir apps/web test -- src/app/admin/components/inputs/rich-text-editor/tiptap-editor.quick-toolbar.test.tsx`.
- [x] 3.4 Update `apps/web/src/app/admin/components/inputs/rich-text-editor/tiptap-editor.tsx` to compose the insertion-target helper, shared image dialog, Quick Toolbar, and an accessible existing-YouTube-contract dialog. Attach context-menu handling only to editable content with a resolvable `posAtCoords`, map pending targets across transactions, restore editor focus/selection after terminal paths, and invoke the existing TipTap YouTube and default code-block commands. Make task 3.3 pass.

## 4. Connect the persistent toolbar to the shared image workflow

- [x] 4.1 Add RED coverage in `apps/web/src/app/admin/components/inputs/rich-text-editor/toolbar/toolbar.test.tsx` (or extend the focused TipTap integration test if Toolbar requires its editor host) proving the persistent Image control opens the shared existing-media dialog, supports multiple local files, inserts through the shared target callback, and retains non-empty image-URL insertion without changing an empty/cancelled request. Verify the selected targeted test file with `pnpm --dir apps/web test -- <test-file>`.
- [x] 4.2 Refactor `apps/web/src/app/admin/components/inputs/rich-text-editor/toolbar/toolbar.tsx` to delegate media selection, multi-file local upload, and image URL insertion to the `TipTapEditor` controller while retaining the persistent Image control and URL UI. Remove the divergent single-file upload loop, do not alter unrelated toolbar actions, and make task 4.1 pass.

## 5. Verify the implementation and change readiness

- [x] 5.1 Run the focused editor suite: `pnpm --dir apps/web test -- src/app/admin/components/inputs/rich-text-editor/editor-insertion-target.test.ts src/app/admin/components/inputs/rich-text-editor/image-insertion-dialog.test.tsx src/app/admin/components/inputs/rich-text-editor/quick-toolbar.test.tsx src/app/admin/components/inputs/rich-text-editor/tiptap-editor.quick-toolbar.test.tsx`, plus existing `mermaid-editor.test.tsx`; resolve regressions or record pre-existing failures separately.
- [ ] 5.2 Review the implementation diff and run the affected frontend gates: `pnpm --dir apps/web test`, `pnpm --dir apps/web lint`, and `pnpm --dir apps/web build`; confirm no API contract, generated SeaORM entity, schema, migration, runtime-config, or dependency change was introduced, and record any pre-existing failure separately.
- [ ] 5.3 Run the required repository gates before handoff: `cargo check`, `cargo test`, `cargo fmt -- --check`, and `cargo clippy`; report any existing API-test failure separately from this web-only change.
- [ ] 5.4 Run `openspec verify --change "add-admin-editor-quick-toolbar"` and `openspec status --change "add-admin-editor-quick-toolbar" --json`; resolve implementation-plan conflicts before checking off completed tasks and hand off the verification evidence to the Software Engineer/Release Engineer as applicable.
