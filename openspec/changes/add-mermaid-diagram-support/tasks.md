## 1. Shared safe rendering foundation

- [ ] 1.1 Prerequisite: read all change artifacts, obtain implementation-time PD guidance for the toolbar/Article Preview/source disclosure, and run `get_minimal_context(task="add-mermaid-diagram-support")`. Add failing policy/parser tests in `packages/editor-prose/src/__tests__/mermaid-renderer.test.ts`, then add exact `mermaid: "11.17.2"` and `src/mermaid-renderer.ts` with D1/D4 strict configuration, directive/frontmatter rejection, size limits, cached browser-only loader, serialized render jobs, and categorized failures. Verify ordinary flowchart/sequence parsing with the real pinned library plus policy cases with mocks using `pnpm --dir packages/editor-prose test -- src/__tests__/mermaid-renderer.test.ts` and `pnpm --dir packages/editor-prose typecheck`. Inspect resolved engine/advisory findings before accepting the version; return a material compatibility conflict to SA.
- [ ] 1.2 After 1.1, add failing shared component tests in `packages/editor-prose/src/__tests__/mermaid-prose.test.tsx`, then extend `src/ArticleProse.tsx` and add `src/mermaid-enhancement.ts` for exact marker recognition, per-root source capture, lazy loading, SVG-image presentation, source disclosure, and local fallback. Cover valid neighbors beside invalid/empty/unsupported/oversized blocks, ordinary HTML unchanged, and no runtime load for plain articles. Verify with `pnpm --dir packages/editor-prose test` and `pnpm --dir packages/editor-prose typecheck`; retain the existing `article-prose.test.tsx` fixture.
- [ ] 1.3 After 1.2, add failing delayed-result/cleanup tests to `mermaid-prose.test.tsx`, then implement generation guards, unique IDs, queue recovery, the loading deadline, Blob URL revocation, and temporary-container cleanup. Exercise two instances, StrictMode, content replacement, same-HTML rerender, unmount, import rejection followed by successful remount, and valid correction after failure. Verify with `pnpm --dir packages/editor-prose test -- src/__tests__/mermaid-prose.test.tsx`; confirm failed jobs do not poison the queue or mutate unrelated DOM.

## 2. Admin source authoring and interchange

- [ ] 2.1 After 1.2, add failing authoring tests in `apps/web/src/app/admin/components/inputs/rich-text-editor/mermaid-editor.test.tsx`, then add the Mermaid action in `toolbar/toolbar.tsx` and plain-text Mermaid lowlight registration in `tiptap-editor.tsx`. Verify insertion, designation without source loss, focus, undo/redo, read-only restrictions, ordinary language highlighting, and HTML save/reload representation with `pnpm --dir apps/web test -- src/app/admin/components/inputs/rich-text-editor/mermaid-editor.test.tsx`.
- [ ] 2.2 After 2.1, add failing round-trip cases in `.../rich-text-editor/mermaid-interchange.test.ts`, then extract/update Markdown helpers used by `toolbar.tsx` to retain Mermaid boundary whitespace, LF-normalized text, Unicode, entities, and embedded backtick runs. Verify actual marked/Turndown behavior and HTML-edit application produce `language-mermaid` with unchanged decoded source using `pnpm --dir apps/web test -- src/app/admin/components/inputs/rich-text-editor/mermaid-interchange.test.ts`.
- [ ] 2.3 After 1.3 and 2.2, extend admin editor integration coverage for Article Preview in `mermaid-editor.test.tsx` and retain `apps/web/src/__tests__/editor-prose.test.tsx`. Verify preview does not dirty the form or replace source with SVG/Blob URLs; invalid diagrams leave save/publish behavior unchanged; base and translation editor instances preserve their separate values. Fix touched preview dialog focus/label behavior as guided by PD. Verify with `pnpm --dir apps/web test -- src/app/admin/components/inputs/rich-text-editor/mermaid-editor.test.tsx src/__tests__/editor-prose.test.tsx`.

## 3. Public reader, accessibility, and SSR

- [ ] 3.1 After 1.3, add failing cases to `apps/ducth-dev-website/src/pages/pages.test.tsx` and new `src/lib/i18n/getLocalizedPost.test.ts`, then exclude Mermaid source in `PostDetailPage.tsx` highlighting and narrowly recognize diagram-only translations in `getLocalizedPost.ts`. Cover mixed ordinary/Mermaid code, multiple diagrams, existing saved marked content, locale/route replacement, exact class-token recognition, and existing missing/empty translation fallback. Verify with `pnpm --dir apps/ducth-dev-website test -- src/pages/pages.test.tsx src/lib/i18n/getLocalizedPost.test.ts`.
- [ ] 3.2 After 1.3 and 3.1, add tests for SSR/initial hydration/source disclosure to shared `mermaid-prose.test.tsx` and reader `src/pages/__tests__/prose-package.test.tsx`. Implement optional localized presentation labels in ArticleProse, pass English/Vietnamese labels from the reader, and add scoped diagram/source/focus/overflow rules in `packages/editor-prose/src/article-prose.css`. Verify `renderToString` without window/document or Mermaid execution, hydration without mismatch warnings, source availability before enhancement, accessible image/disclosure/status, and unchanged shared root using `pnpm --dir packages/editor-prose test` and `pnpm --dir apps/ducth-dev-website test -- src/pages/__tests__/prose-package.test.tsx`. Do not attempt to prove layout or SVG security using jsdom alone.

## 4. Consumer packaging, browser evidence, and handoff

- [ ] 4.1 After groups 1–3, regenerate `packages/editor-prose/pnpm-lock.yaml`, `apps/web/pnpm-lock.yaml`, and `apps/ducth-dev-website/pnpm-lock.yaml` through pnpm installs; reinstall each app's local file dependency. Update `packages/editor-prose/README.md` with source format, authoring/preview usage, rejected configuration, limits, fallback, browser-only enhancement, and consumer update instructions. Verify frozen installs and dependency resolution in the existing Node 20 build environment, then build both apps. Record real-browser checks at 360px and desktop, both themes and locales: source/preview/save/reopen, keyboard focus/disclosure, multiple and invalid diagrams, hostile script/link/config fixtures with no execution/navigation, no runtime request on a plain article, only local rendering assets, JS-disabled source, missing chunk fallback, valid recovery, and maximum-size responsiveness. Verify deployed CSP compatibility with Blob images and record evidence without article source in logs. For browser failures add a regression test at the appropriate layer before fixing; do not mark this task complete on mocked rendering alone.
- [ ] 4.2 After 4.1, run `pnpm --dir packages/editor-prose test`, `pnpm --dir packages/editor-prose typecheck`, `pnpm --dir apps/web test`, `pnpm --dir apps/web lint`, `pnpm --dir apps/web build`, `pnpm --dir apps/ducth-dev-website test`, `pnpm --dir apps/ducth-dev-website lint`, `pnpm --dir apps/ducth-dev-website typecheck`, and `pnpm --dir apps/ducth-dev-website build` (both client/server output). Run repository gates `cargo check`, `cargo test`, `cargo fmt -- --check`, and `cargo clippy`; report pre-existing/environment failures separately. Validate with `openspec validate add-mermaid-diagram-support --strict` and `openspec status --change add-mermaid-diagram-support --json`. Review final diff and report graph impact evidence, outcome/scenario coverage, version/chunk/build results, risks, and remaining limitations. Hand operational evidence to RE with website-before-admin rollout, no migration, source-preserving rollback, asset retention and CSP checks; do not deploy or archive from this task.

After every implementation group, run graph `detect_changes`, `get_affected_flows`, `tests_for` for the changed renderer/editor/localization functions, and `get_impact_radius`. If graph coverage is unavailable or incomplete, record the limitation and substitute targeted imports/callers search, `git diff`, and relevant tests. Mark each checkbox only after its verification passes. Artifact completion does not mark implementation complete.

## Verification status — 2026-09-14

The implementation is present in commits `0e9ffb7`, `6494907`, and `194a122`.

- Completed local verification: `packages/editor-prose` tests and typecheck; all admin tests and build; all public website tests and client/server build; `cargo fmt -- --check`, `cargo check`, `cargo test`, and `cargo clippy` from `apps/api`; and strict OpenSpec validation.
- The public website test suite required local port binding. It passed when run outside the filesystem sandbox; the Mermaid lazy chunk added an `async/` directory, so the production bundle assertion now reads JavaScript files recursively.
- Existing unrelated checks remain: the admin ESLint flat-config incompatibility, and missing declarations for `head-assets.mjs` and `server-config.mjs` in the website typecheck. Rust Clippy emits existing warnings.
- Remaining task evidence: a frozen install/build in the Node 20 container, browser checks at mobile and desktop widths in both themes/locales, JavaScript-disabled and missing-chunk recovery, deployed CSP compatibility for Blob image URLs, and the dependency advisory review. The advisory command was not run because its external request would export the dependency inventory and was rejected by approval review.

The unchecked tasks accurately retain those outstanding requirements; they are not implementation failures.
