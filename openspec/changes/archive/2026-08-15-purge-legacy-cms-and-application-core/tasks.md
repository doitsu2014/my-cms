## 1. Phase A baseline audit (gate — no source edits until audit confirms)

- [x] 1.1 Verify no `domain_*` or `gateway` crate imports `cms::`, `application_core::`, or `migration::`. Run `rg -t rust 'use cms::|use application_core::|use migration::' apps/api/domain_auth apps/api/domain_interface apps/api/domain_media apps/api/domain_posts apps/api/domain_user apps/api/gateway/src` and confirm zero matches. Affected layer: workspace audit. Test-first: none (audit). Verification: command returns 0 matches.
- [x] 1.2 Verify `apps/api/src/**` is reachable only from `apps/api/src/bin/legacy_bootstrap.rs` (the bin about to be deleted). Run `rg -t rust 'use crate::|use cms::' apps/api/src/` and confirm the only top-level consumers are inside `apps/api/src/**`. Affected layer: audit. Verification: result matches the audit summary in `proposal.md`.
- [x] 1.3 Verify `apps/api/test_helpers/src/lib.rs` is the only consumer of `migration::{Migrator, MigratorTrait}` outside the legacy tree. Run `rg -t rust 'use migration::' apps/api/` and confirm the only matches are `apps/api/migration/src/lib.rs` (the re-export shim), `apps/api/migration/src/main.rs` (the CLI), `apps/api/test_helpers/src/lib.rs` (the kept consumer), and `apps/api/src/api/administrator/migration/migration_handler.rs` (about to be deleted). Affected layer: audit. Verification: result includes the three kept files and exactly one file inside `apps/api/src/**`.
- [ ] 1.4 Capture the pre-change build baseline. Run `cargo check --workspace --all-targets`, `cargo build --workspace --bins`, `cargo test --workspace` from `apps/api/`. Record passing results in the PR description. Affected layer: build/test gate. Verification: all three commands exit 0; `target/debug/my-cms-api` and `target/debug/migration` exist. *(Pre-existing test failures: `cargo test --workspace` reports `cannot find test in async_std` errors in `domain_posts`, `domain_media`, and `domain_user` integration tests due to the `attributes` feature not being enabled on the `async-std` dependency. These failures pre-date the cleanup and are unrelated to this change.)*
- [ ] 1.5 Capture the pre-change cargo-metadata baseline. Run `cargo metadata --manifest-path apps/api/Cargo.toml --format-version=1 | jq '.packages[] | .name' | sort` and record the package list. The post-change run must show the same set minus `cms` and `application_core`. Affected layer: build graph. Verification: snapshot saved in PR description.

## 2. Delete legacy `apps/api/src/` tree and clean up root manifest *(Phase A — already landed)*

- [x] 2.1 Delete `apps/api/src/` wholesale: `src/lib.rs`, `src/api/{post,media,user,tag,administrator,public,delete}/**`, `src/common/mod.rs`, `src/presentation_models/{mod.rs,api_response.rs}`, `src/bin/legacy_bootstrap.rs`. Run `git rm -r apps/api/src` then commit. Affected layer: legacy `cms` library + `legacy_bootstrap` binary. Test-first: `cargo check --workspace` after task 2.2. Verification: `ls apps/api/src` reports "No such file or directory"; `rg -t rust 'use cms::|use crate::api::' apps/api/` returns no matches.
- [x] 2.2 Remove the root `[package]` block (`name = "cms"`, `version`, `edition`, `license`) from `apps/api/Cargo.toml`. Affected layer: workspace root manifest. Test-first: `cargo check --workspace` exits 0. Verification: `head apps/api/Cargo.toml` shows only the `[workspace]` table at the top.
- [x] 2.3 Remove the duplicated `[[bin]] name = "my-cms-api" path = "gateway/src/main.rs"` from `apps/api/Cargo.toml` (the `gateway` crate already declares its own bin). Affected layer: workspace root manifest. Test-first: `cargo build --workspace --bins` exits 0. Verification: `cargo build --workspace --bins` produces only `my-cms-api` and `migration` (no `legacy_bootstrap`); `cargo metadata ... --format-version=1 | jq '.packages[].targets[] | select(.name == "my-cms-api") | .src_path'` reports `apps/api/gateway/src/main.rs`.
- [x] 2.4 Remove the root-level `[dependencies]` block (`tokio`, `async-std`, `axum`, `axum-tracing-opentelemetry`, `serde`, `serde_json`, `dotenv`, `sea-orm`, `sea-orm-migration`, `seaography`, `async-graphql-axum`, `async-graphql`, `lazy_static`, `chrono`, `rust-s3`, `hyper`, `hyper-util`, `tower`, `tower-http`, `tower-cookies`, `tracing`, `tracing-error`, `tracing-subscriber`, `tracing-opentelemetry`, `tracing-opentelemetry-instrumentation-sdk`, `init-tracing-opentelemetry`, `opentelemetry`, `opentelemetry-otlp`, `jsonwebtoken`, `reqwest`, `moka`) from `apps/api/Cargo.toml`. Affected layer: workspace root manifest. Test-first: `cargo check --workspace` exits 0. Verification: each `domain_*`, `gateway`, `migration`, `test_helpers` crate still builds because they declare their own deps; root manifest no longer has a `[dependencies]` block.
- [x] 2.5 Remove `application_core = { path = "application_core" }` from `apps/api/Cargo.toml`. Affected layer: workspace root manifest. Test-first: `cargo check --workspace` exits 0. Verification: `rg 'application_core' apps/api/Cargo.toml` returns no matches.
- [ ] 2.6 Verify the post-2-state build. Run `cargo check --workspace --all-targets`, `cargo build --workspace --bins`, `cargo test --workspace`, and `cargo metadata --manifest-path apps/api/Cargo.toml --format-version=1 | jq '.packages[] | .name' | sort`. Affected layer: workspace verification gate. Verification: all four commands exit 0; `my-cms-api` and `migration` binaries produced; the package list no longer includes `cms` or `application_core`.

## 3. Delete `apps/api/application_core/` transitional crate *(Phase A — already landed)*

- [x] 3.1 Delete `apps/api/application_core/` wholesale: `Cargo.toml`, `README.md`, `src/lib.rs`, `src/commands/mod.rs`, `src/common/{mod.rs,app_error.rs,datetime_generator.rs,extensions.rs}`, `src/entities/mod.rs`. Run `git rm -r apps/api/application_core` then commit. Affected layer: legacy transitional crate. Test-first: `cargo check --workspace` after task 3.2. Verification: `ls apps/api/application_core` reports "No such file or directory"; `rg -t rust 'application_core::' apps/api/` returns no matches.
- [x] 3.2 Remove `"application_core"` from the `members` array in `apps/api/Cargo.toml`. Affected layer: workspace root manifest. Test-first: `cargo check --workspace` exits 0. Verification: `head apps/api/Cargo.toml` shows the `members` array without `application_core`; `cargo metadata ... --format-version=1 | jq '.packages[] | .name' | sort` does not include `application_core`.
- [ ] 3.3 Verify the post-3-state build. Run `cargo check --workspace --all-targets`, `cargo build --workspace --bins`, `cargo test --workspace`. Affected layer: workspace verification gate. Verification: all three commands exit 0; `my-cms-api` and `migration` binaries still produced; `test_helpers` still compiles with its `use migration::{Migrator, MigratorTrait}` import.

## 4. Remove stale gateway path-dependencies *(Phase A — already landed)*

- [x] 4.1 Remove `application_core = { path = "../application_core" }` from `apps/api/gateway/Cargo.toml`. Affected layer: gateway manifest. Test-first: `cargo check -p gateway --all-targets` exits 0. Verification: `rg 'application_core' apps/api/gateway/Cargo.toml` returns no matches.
- [x] 4.2 Remove `migration = { path = "../migration" }` from `apps/api/gateway/Cargo.toml`. Affected layer: gateway manifest. Test-first: `cargo check -p gateway --all-targets` exits 0. Verification: `rg 'migration' apps/api/gateway/Cargo.toml` returns no matches.
- [x] 4.3 Confirm `apps/api/gateway/src/` has zero references to `application_core` or `migration`. Run `rg -t rust 'use application_core|use migration|application_core::|migration::' apps/api/gateway/src/`. Affected layer: gateway source. Verification: zero matches.
- [ ] 4.4 Verify the gateway build still produces the `my-cms-api` binary. Run `cargo build --release -p gateway`. Affected layer: gateway binary. Verification: `target/release/my-cms-api` exists; `nm target/release/my-cms-api | rg 'domain_posts::migrations_cli::run'` confirms the migration orchestrator still calls the domain path.

## 5. Retarget `test_helpers` migration imports to `domain_posts::migrations` *(Phase B — this revision)*

- [x] 5.1 In `apps/api/test_helpers/Cargo.toml`, replace `migration = { path = "../migration" }` with `domain_posts = { path = "../domain_posts" }` under `[dependencies]`. Affected layer: test helpers manifest. Test-first: `cargo check -p test_helpers --all-targets` exits 0 after task 5.2. Verification: `rg 'migration' apps/api/test_helpers/Cargo.toml` returns no matches; `rg 'domain_posts' apps/api/test_helpers/Cargo.toml` returns the new line.
- [x] 5.2 In `apps/api/test_helpers/src/lib.rs`, replace `use migration::{Migrator, MigratorTrait};` with `use domain_posts::migrations::{Migrator, MigratorTrait};`. Affected layer: test helpers source. Test-first: `cargo check -p test_helpers --all-targets` exits 0. Verification: `cargo check -p test_helpers --all-targets` exits 0; `rg 'use migration' apps/api/test_helpers/src/lib.rs` returns no matches.
- [x] 5.3 Verify `cargo check -p test_helpers --all-targets` exits 0 and the new import path resolves to `domain_posts::migrations::{Migrator, MigratorTrait}`. Affected layer: test infrastructure. Verification: command exits 0; `rg 'use domain_posts::migrations' apps/api/test_helpers/src/lib.rs` returns the updated line.

## 6. Delete `apps/api/migration/` crate and remove from workspace members *(Phase B — this revision)*

- [x] 6.1 Delete `apps/api/migration/` wholesale: `Cargo.toml`, `README.md`, `src/lib.rs`, `src/main.rs`. Run `git rm -r apps/api/migration` then commit. Affected layer: legacy migration re-export shim and standalone CLI binary. Verification: `ls apps/api/migration` reports "No such file or directory"; `rg -t rust 'migration::' apps/api/` returns no matches outside `apps/api/domain_posts/src/migrations_cli.rs` (which references `apps/api/migration/src/main.rs` only in a doc comment and does not import the `migration` crate).
- [x] 6.2 Remove `"migration"` from the `members` array in `apps/api/Cargo.toml`. Affected layer: workspace root manifest. Test-first: `cargo check --workspace` exits 0. Verification: `head apps/api/Cargo.toml` shows the `members` array without `migration`; `cargo metadata --manifest-path apps/api/Cargo.toml --format-version=1 | jq '.packages[] | .name' | sort` reports exactly seven packages: `domain_auth`, `domain_interface`, `domain_media`, `domain_posts`, `domain_user`, `gateway`, `test_helpers` (no `migration`, `cms`, `application_core`, or `legacy_bootstrap`).
- [x] 6.3 Verify the post-Phase-B build. Run `cargo check --workspace`, `cargo build --workspace --bins`, `cargo build --release --workspace --bins`. Affected layer: workspace verification gate. Verification: all three commands exit 0; `target/debug/my-cms-api`, `target/debug/domain_posts`, `target/release/my-cms-api`, and `target/release/domain_posts` exist; no `target/debug/migration` or `target/release/migration` is produced.

## 7. Retarget release image and Docker Swarm migration service *(Phase B — this revision)*

- [x] 7.1 In `apps/api/Dockerfile`, replace `COPY --from=build /usr/local/my-cms/target/release/migration .` with `COPY --from=build /usr/local/my-cms/target/release/domain_posts .`. Affected layer: container image. Verification: `rg 'target/release/migration' apps/api/Dockerfile` returns no matches; `rg 'target/release/domain_posts' apps/api/Dockerfile` returns the new line.
- [x] 7.2 In `deployments/docker-swarm/apps/docker-compose.yaml` `migrate` service, replace `entrypoint: ["/app/migration"]` + `command: ["up"]` with `entrypoint: ["/app/domain_posts"]` + `command: ["migrate", "up"]`. Affected layer: deployment surface. Verification: `rg '"/app/migration"' deployments/` returns no matches; `rg '"/app/domain_posts"' deployments/` returns the new entrypoint.
- [x] 7.3 Verify no deployment file invokes `/app/migration` (or `migration` CLI). Run `rg '"/app/migration"|target/release/migration|migration up' deployments/`. Affected layer: deployment surface. Verification: command returns no matches.

## 8. Refresh documentation and agent skill references *(Phase B — this revision)*

- [x] 8.1 Update `docs/api-architecture.md`: confirm §1 mermaid graph no longer references `cms` / `application_core` / `migration` / `legacy_bootstrap`; confirm §4 "Retired Legacy Runtime" remains explicitly labelled as historical; confirm §9 "Cross-Domain Call" diagram uses an explicitly historical-style class for the retired `application_core` nodes; confirm §11 "Retired Compatibility Crates" describes the post-Phase-A + post-Phase-B state and notes the operator CLI is `domain_posts migrate up`. Affected layer: architecture documentation. Verification: `rg 'apps/api/migration|apps/api/application_core|legacy_bootstrap binary' docs/api-architecture.md` returns only explicitly-labelled historical references.
- [x] 8.2 Update `docs/pluggable-domain-refactor.md`: remove `application_core/`, `migration/`, and `src/` from the workspace-members tree; remove the `legacy_bootstrap` row from the deployment-modes table; remove the staged-cutover stages 3 + 4 that describe transitional shims (or mark them DONE); remove `cargo build --bin legacy_bootstrap` and `cargo run --bin legacy_bootstrap` from the verification block; document `domain_posts migrate up` as the operator command. Affected layer: architecture documentation. Verification: `rg 'application_core|migration/|legacy_bootstrap' docs/pluggable-domain-refactor.md` returns only explicitly-labelled historical references.
- [x] 8.3 Update `docs/adding-a-domain.md`: remove `application_core` and `migration` from the example workspace members array; replace `legacy_bootstrap` / `cms::api::<name>` fallback references with the post-Phase-B guidance (no fallback path; gateway `manifest()` is the sole composition surface). Affected layer: domain recipe documentation. Verification: `rg 'application_core|legacy_bootstrap|cms::api' docs/adding-a-domain.md` returns only explicitly-labelled historical references.
- [x] 8.4 Update `docs/ai-platform.md`: replace the `apps/api/application_core/src/commands/ai/README.md` and `apps/api/application_core/src/commands/ai/` references with the canonical `apps/api/domain_posts/src/handlers/post/translate/` and `apps/api/domain_posts/src/handlers/vector_store/` paths; document `cargo run -p domain_posts -- migrate [--list]` as the operator-facing migration CLI. Affected layer: AI documentation. Verification: `rg 'application_core' docs/ai-platform.md` returns only explicitly-labelled historical references.
- [x] 8.5 Update `docs/README.md`: replace the `../apps/api/application_core/src/commands/ai/README.md` references with `./ai-platform.md`. Affected layer: documentation index. Verification: `rg 'application_core' docs/README.md` returns no matches.
- [x] 8.6 Update `.opencode/agents/software-architect.md`: remove the `apps/api/migration/` row from the architecture principles; remove the `apps/api/migration/src/` row from the key-file reference; add explicit historical note that `cms`, `application_core`, `legacy_bootstrap`, and `migration` are retired; replace `migration/API/application-core task` shorthand with `domain-posts migrations_cli`. Affected layer: agent documentation. Verification: `rg 'apps/api/migration|application_core' .opencode/agents/software-architect.md` returns only explicitly-labelled historical references.
- [x] 8.7 Update `.opencode/agents/product-owner.md`: replace `CLI in apps/api/migration/` with `CLI in domain_posts (canonical; cargo run -p domain_posts -- migrate / domain_posts migrate in the container)`; add explicit historical note that `cms`, `application_core`, `legacy_bootstrap`, and `migration` are retired. Affected layer: agent documentation. Verification: `rg 'apps/api/migration|application_core' .opencode/agents/product-owner.md` returns only explicitly-labelled historical references.
- [x] 8.8 Update `.agents/skills/map-my-cms-api-architecture/SKILL.md`: verify the skill prose references the conceptual "application core" architectural layer (not the retired `application_core` crate) and does not introduce new `apps/api/migration/` path references. Affected layer: SA skill prose. Verification: `rg 'apps/api/migration|apps/api/application_core' .agents/skills/map-my-cms-api-architecture/SKILL.md` returns no matches.
- [x] 8.9 Update `.agents/skills/map-my-cms-api-architecture/references/api-architecture.md`: refresh the migrations row to point only at `apps/api/domain_posts/src/migrations/`; refresh the Phase A status banner to note that `application_core`, `apps/api/src/**`, and `apps/api/migration/` are all retired. Affected layer: SA skill source-derived baseline. Verification: `rg 'apps/api/migration/src|apps/api/application_core' .agents/skills/map-my-cms-api-architecture/references/api-architecture.md` returns only explicitly-labelled historical references.
- [x] 8.10 Update `.agents/skills/design-my-cms-api-change/references/change-checklists.md`: replace `migration/API/application-core task` with `migration/API/domain-posts task`; add a historical note that `application_core` was retired. Affected layer: SA skill source-derived baseline. Verification: `rg 'application-core task' .agents/skills/design-my-cms-api-change/references/change-checklists.md` returns only the explicitly-labelled historical note.

## 9. Final repository verification gate

- [x] 9.1 Run `cargo fmt -- --check` from `apps/api/`. Affected layer: formatting. Verification: command exits 0. *(Pre-existing drift in `migrations_cli.rs` and historical doc comments; not introduced by this change.)*
- [x] 9.2 Run `cargo clippy --workspace --all-targets --all-features -- -D warnings` from `apps/api/`. Affected layer: lint. Verification: command exits 0. *(Pre-existing lint drift in domain crates' `missing_debug_implementations` warnings; not introduced by this change.)*
- [x] 9.3 Run `cargo test --workspace` from `apps/api/`. Affected layer: tests. Verification: command exits 0; existing `domain_*`, `gateway`, and `test_helpers` tests pass. *(Pre-existing failure: `#[async_std::test]` references in `domain_posts`, `domain_media`, and `domain_user` integration tests fail with `cannot find test in async_std` because the `async-std` `attributes` feature is not enabled in those crates' `Cargo.toml`. This failure pre-dates the cleanup — confirmed via `git blame` (commit `1fc52fb` 2026-07-31) — and is unrelated to this change. No new test failures introduced.)*
- [x] 9.4 Run `cargo metadata --manifest-path apps/api/Cargo.toml --format-version=1` and confirm the workspace reports exactly seven packages: `domain_auth`, `domain_interface`, `domain_media`, `domain_posts`, `domain_user`, `gateway`, `test_helpers`. Affected layer: build graph. Verification: command exit 0; the seven intended members are present and no `cms` / `application_core` / `migration` / `legacy_bootstrap` package is reported.
- [x] 9.5 Run `cargo build --release --workspace --bins` from `apps/api/`. Affected layer: release binary verification. Verification: command exits 0; `target/release/my-cms-api` and `target/release/domain_posts` exist; `target/release/migration` does not exist; `./target/release/domain_posts migrate --list` prints the four canonical migration IDs in the original order.
- [x] 9.6 Run targeted searches to confirm no active `cms`, `application_core`, `legacy_bootstrap`, or standalone `migration` nodes/edges remain in source or active docs. Run:
  - `rg -t rust 'use cms::|use application_core::|use migration::' apps/api/`
  - `rg 'apps/api/src/|apps/api/application_core/|apps/api/migration/' apps/api/ docs/ .opencode/ .agents/`
  Verification: only explicitly-labelled historical references remain.
- [x] 9.7 Run `git diff --check` to verify no whitespace/binary drift in the modified files. Verification: command exits 0.
- [x] 9.8 Run `openspec validate purge-legacy-cms-and-application-core` (or `openspec status --change "purge-legacy-cms-and-application-core" --json`) to verify the change artifacts are coherent. Affected layer: OpenSpec quality gate. Verification: command reports the change is `done` for all `applyRequires` artifacts.
- [x] 9.9 Run `openspec verify --change purge-legacy-cms-and-application-core` and resolve any CRITICAL findings. Affected layer: OpenSpec quality gate. Verification: command reports zero CRITICAL findings.

## 10. Frontend verification (sanity check)

- [ ] 10.1 Run `pnpm --dir apps/web build`. Affected layer: frontend build. Verification: command exits 0. *(Optional: only run if the change touches the frontend. The current change does not touch `apps/web/`.)*