## 1. Trace regression coverage (RED)

- [x] 1.1 [Website observability tests — new server-only telemetry test module under `apps/ducth-dev-website/src/`] Add deterministic tests for the opt-in telemetry bootstrap/config seam using an in-memory exporter or equivalent local test provider. Before runtime behavior is added, prove: disabled/absent/invalid `ENABLED_OTLP_EXPORTER` is no-op and fail-open; valid enabled configuration creates `service.name=ducth-dev-website`; only the configured bounded route/status/error attributes are finished; and all forbidden content, header, cookie, authorization, URL-query, GraphQL document/variable, rendered-HTML, and environment-value data are absent. (Requirements: opt-in export; privacy-safe telemetry. Design decisions 1 and 3.)
- [x] 1.2 [Website production-server integration test — `apps/ducth-dev-website/src/test/production-server.test.ts`] Extend the real built-server test seam with an enabled telemetry configuration and a local GraphQL stub that captures request headers. Make it fail against the uninstrumented server by asserting `GET /en` sends a syntactically valid W3C `traceparent`, emits bounded website server/client spans with one trace ID and child parentage, and preserves existing SSR/GraphQL status behavior. Add a valid inbound `traceparent` case that proves the downstream request continues the caller trace. (Requirement: SSR request and GraphQL trace-context propagation. Design decisions 2 and 4.)
- [x] 1.3 [Health and browser regression tests — `apps/ducth-dev-website/src/test/production-server.test.ts` and focused source/build assertions] Add failing coverage showing that enabled `GET /healthz` produces zero finished website spans and zero GraphQL requests, while the browser client entry/bundle does not initialize the telemetry bootstrap, exporter, or traced server fetch wrapper. Retain existing shallow-health and `/en` compatibility assertions. (Requirement: shallow health probing and privacy-safe telemetry. Design decisions 2 and 3.)

## 2. Server-side telemetry implementation (GREEN → REFACTOR)

- [x] 2.1 [Website dependencies — `apps/ducth-dev-website/package.json`, `apps/ducth-dev-website/pnpm-lock.yaml`] Add only the Node OpenTelemetry API, SDK, OTLP/HTTP trace exporter, and directly required supporting packages at mutually compatible versions. Do not add browser SDKs, broad auto-instrumentation bundles, analytics packages, or an exporter credential. Verify the lockfile is updated with `pnpm install --lockfile-only` or the repository's equivalent deterministic dependency command. (Requirements: opt-in export; browser behavior remains uninstrumented. Design decision 1.)
- [x] 2.2 [Website telemetry boundary — new server-only telemetry module(s) under `apps/ducth-dev-website/src/` plus `apps/ducth-dev-website/instrumentation.mjs`] Implement the startup-time opt-in Node SDK bootstrap with explicit W3C propagation, OTLP/HTTP trace exporter, sampler/service configuration, a no-op/fail-open path, bounded sanitized warnings, and idempotent shutdown flush handling. Expose narrowly scoped helpers for extracting an inbound parent, executing a bounded SSR server span, and executing/injecting a bounded GraphQL client span so tests can use an in-memory exporter. Do not attach unsafe attributes or enable competing automatic HTTP/Express/Undici instrumentation. (Requirements: opt-in export; propagation; privacy. Design decisions 1–3.)
- [x] 2.3 [SSR request boundary — `apps/ducth-dev-website/server.prod.mjs`] Run the existing catch-all SSR/template/response flow inside the new server-span helper while preserving route order, `/healthz`'s exact shallow response, static assets, root redirect, generic 500 behavior, and correlation logging. Ensure the server never creates a span for `/healthz`, records only stable route/method/protocol/status/error-category data for reader traffic, and continues valid incoming W3C context. (Requirements: propagation; shallow health and privacy. Design decision 2.)
- [x] 2.4 [Apollo transport boundary — `apps/ducth-dev-website/src/infrastructure/graphql/graphql-client.ts` and any server-only helper] Configure only the SSR Apollo `HttpLink` to use the traced fetch wrapper. Preserve the existing URL resolver, Apollo cache behavior, network-only SSR policy, GraphQL request method/body/variables, and browser client fetch behavior. The wrapper must inject the active `traceparent`, create a child client span, allowlist endpoint/method/status/error attributes, and never log or attach GraphQL payloads or headers. (Requirement: SSR request and GraphQL trace-context propagation; privacy. Design decisions 2 and 3.)
- [x] 2.5 [Bootstrap launch path — `apps/ducth-dev-website/package.json`, `apps/ducth-dev-website/Dockerfile`, and production-server test launch helper] Ensure every production invocation preloads `instrumentation.mjs` before `server.prod.mjs` using Node ESM `--import`; copy the bootstrap into the runner image without changing the non-root user, health-check command, port, or current runner behavior. Keep tests able to start both enabled and disabled processes through the same ordering. (Requirement: opt-in export. Design decisions 1 and 2.)

## 3. Deployment configuration and documentation

- [x] 3.1 [Apps Compose service — `deployments/docker-swarm/apps/docker-compose.yaml`] Add website-only mappings from `WEBSITE_ENABLED_OTLP_EXPORTER`, `WEBSITE_OTEL_SERVICE_NAME`, `WEBSITE_OTEL_EXPORTER_OTLP_TRACES_ENDPOINT`, and `WEBSITE_OTEL_TRACES_SAMPLER` to the standard server-only runtime OTel variables. Default tracing to disabled, the identity to `ducth-dev-website`, and the Node OTLP/HTTP trace endpoint to `http://jaeger:4318/v1/traces`; preserve API and Jaeger service configuration, website network membership, health check, and public routing. (Requirement: opt-in server-side website trace export. Design decision 1.)
- [x] 3.2 [Environment documentation — `deployments/docker-swarm/apps/.env.example` and `apps/ducth-dev-website/.env.example`] Document the website tracing flag, service name, OTLP/HTTP trace endpoint, and sampler with safe defaults and clarify that these are server-only runtime values not to be embedded in website browser configuration. Preserve all required `WEBSITE_*` reader configuration and avoid copying the API's gRPC endpoint or service identity. (Requirement: opt-in export. Design decisions 1 and 3.)

## 4. Verification and release handoff

- [x] 4.1 [Focused verification] Run the new telemetry/config tests and `src/test/production-server.test.ts`, then `pnpm --dir apps/ducth-dev-website test`, `pnpm --dir apps/ducth-dev-website typecheck`, `pnpm --dir apps/ducth-dev-website lint`, and `pnpm --dir apps/ducth-dev-website build`. Review the diff and record pre-existing unrelated failures separately. Confirm enabled `/en` propagation/span parentage, disabled fail-open behavior, zero `/healthz` spans/GraphQL traffic, and the absence of prohibited attributes/browser telemetry. (Requirements: all; Design decision 4.)
- [ ] 4.2 [Container and configuration verification] Build the Ducth website Docker image and run `docker compose -f deployments/docker-swarm/apps/docker-compose.yaml --env-file deployments/docker-swarm/apps/.env.example config`. Inspect that the website process uses the ESM preload, its health check remains `/healthz`, its resolved service name/OTLP endpoint are website-specific, and no API/Jaeger/Traefik topology changes are introduced. (Requirements: opt-in export; shallow health. Migration steps 3–4.)
- [ ] 4.3 [Release Engineer evidence prerequisite] Provide the Release Engineer the image tag/digest, changed dependency/config paths, focused verification outputs, explicit sampler value, and the post-rollout acceptance procedure: deploy only the reader image/config after `restore-gateway-otlp-tracing` is operational; request `/en`; confirm Jaeger shows one trace with `ducth-dev-website` and `my-cms-headless-api` segments linked through `POST /posts/graphql/immutable`; verify `/healthz` is HTTP 200 with no website/API trace; monitor reader error rate, latency, exporter warnings, and trace volume after old tasks drain. Do not deploy without authorization. (Requirements: all; Design decision 4.)
- [ ] 4.4 [Rollback evidence] Document and validate the operator rollback: set `WEBSITE_ENABLED_OTLP_EXPORTER=false` and redeploy the website service, or redeploy the prior reader image. Confirm `/en` and `/healthz` behavior remain normal, no data cleanup is required, and API/Jaeger services are untouched. State rollback triggers: reader startup/failure/latency regression, unexpected sensitive attributes, unacceptable trace volume, or failed exporter configuration. (Design rollback plan.)
