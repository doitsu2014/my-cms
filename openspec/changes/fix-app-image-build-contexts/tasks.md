## 1. Build-input boundaries

- [x] 1.1 [application-image-build] Add `target/` to `apps/api/.dockerignore` without weakening its existing credential and local-artifact exclusions; first create a harmless sentinel beneath `apps/api/target/`, then verify BuildKit does not transfer it with `docker build --progress=plain -t my-cms-api:build-check -f apps/api/Dockerfile apps/api`.
- [x] 1.2 [application-image-build] Add `packages/editor-prose/.dockerignore` that retains the package manifest, lockfile, source, and required TypeScript configuration while excluding `node_modules`, build/test output, environment files, logs, and editor/OS artifacts; verify the named context does not contain a sentinel in an excluded path.

## 2. Frontend Dockerfile contracts

- [x] 2.1 [application-image-build] Update `apps/web/Dockerfile` so its `deps` and `builder` stages receive the named `editor-prose` context at `/packages/editor-prose`; copy `pnpm-lock.yaml` before dependency installation and use `pnpm install --frozen-lockfile`. Preserve the nginx runner contract. Verify first with `docker build --build-context editor-prose=packages/editor-prose -t my-cms-admin:build-check -f apps/web/Dockerfile apps/web`.
- [x] 2.2 [application-image-build, website-deployment] Update `apps/ducth-dev-website/Dockerfile` so both dependency installation and compilation receive the named `editor-prose` context at `/packages/editor-prose`, while retaining its committed-lockfile install, non-root runner, and healthcheck. Verify first with `docker build --build-context editor-prose=packages/editor-prose -t ducth-dev-website:build-check -f apps/ducth-dev-website/Dockerfile apps/ducth-dev-website` and inspect the final image user/healthcheck metadata.
- [x] 2.3 [application-image-build] Add or update a focused build-contract check covering the three direct Docker invocations, required named context, app-scoped primary contexts, frozen admin install, and expected failure when a frontend lacks `editor-prose`; run it before and after the Dockerfile changes and retain actionable image-specific output.

## 3. Compose and CI entry-point alignment

- [x] 3.1 [application-image-build, website-deployment] Update the `my-cms-frontend` and `ducth-dev-website` build blocks in `deployments/docker-swarm/apps/docker-compose.yaml` to declare `additional_contexts.editor-prose: ../../../packages/editor-prose`, preserving their current primary `context`, runtime, network, and routing fields. Verify with `docker compose -f deployments/docker-swarm/apps/docker-compose.yaml config` and the two service-specific Compose builds.
- [x] 3.2 [application-image-build] Update `.github/workflows/release-my-cms-admin-image.yml` to pass `editor-prose=./packages/editor-prose` through the Docker build action and trigger on `packages/editor-prose/**` as well as `apps/web/**`; verify the workflow remains publishing-only in its existing release job and that its Docker input matches the direct admin build.
- [x] 3.3 [application-image-build] Create `.github/workflows/validate-app-image-builds.yml` as a non-publishing pull-request/relevant-push validation that builds API, admin, and Ducth images with their declared bounded contexts and validates the Compose build configuration/path. It MUST not log in, push images, require application secrets, or silently skip a failing image; verify YAML, trigger paths, and image-labelled build steps locally before CI execution.

## 4. Integrated verification and handoff

- [x] 4.1 [application-image-build] Before edits and after the Docker/CI task group, run the required current code-review-graph impact checks for this change if a graph rebuilt for the working branch is available; otherwise document the stale/unavailable-graph fallback using `git diff`, targeted Dockerfile/Compose/workflow searches, caller inspection for API `migrate` and `/healthz`, and test/workflow inspection.
- [x] 4.2 [application-image-build, website-deployment] Run the complete image validation: direct API/admin/Ducth builds, `docker compose -f deployments/docker-swarm/apps/docker-compose.yaml build`, and the focused context-contract checks. Confirm the API context excludes `target/`, both frontend builds resolve the local package, and no primary context is widened to the repository root.
- [x] 4.3 [application-image-build] Run behavior-preservation verification after image builds: `cargo check`, `cargo test`, `cargo fmt -- --check`, and `cargo clippy` for `apps/api`; `pnpm --dir apps/web test` and `pnpm --dir apps/web build`; and `pnpm --dir apps/ducth-dev-website test` and `pnpm --dir apps/ducth-dev-website build`. Record any environmental dependency failure separately from a product regression.
- [x] 4.4 [release handoff] Provide the Release Engineer with image tags/digests, CI results, context-size evidence, unchanged runtime-contract evidence, and rollback instructions. No deployment is authorized by this task; if staging is later authorized, validate API `/healthz`, admin `/health`, Ducth `/en` plus Docker health, and Traefik routes before promotion, reverting together to prior image digests/tags if any check fails.
