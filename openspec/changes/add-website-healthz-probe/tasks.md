## 1. Regression coverage (RED)

- [x] 1.1 [Website runtime/test] First add a node-environment Vitest integration test for the production website server (for example under `apps/ducth-dev-website/src/test/`) that starts it with valid `WEBSITE_*` values and a local GraphQL request counter. Assert `GET /healthz` returns HTTP 200 while the render/dependency path is not invoked (zero GraphQL requests); make the test fail against the current catch-all-only server. Run the build before this test so it exercises `server.prod.mjs` with its real SSR bundle. (Requirement: shallow endpoint scenario; Design decision 1.)
- [x] 1.2 [Container artifact/test] First add a focused test or deterministic artifact assertion that the runner `HEALTHCHECK` retains its current interval, timeout, start-period, retry count, wget behavior, and port but targets `/healthz`, not `/en`; make it fail against the current Dockerfile. Retain existing `/en` SSR tests as compatibility coverage. (Requirement: shallow and reader-compatibility scenarios; Design decision 2.)

## 2. Focused implementation (GREEN → REFACTOR)

- [x] 2.1 [Website runtime — `apps/ducth-dev-website/server.prod.mjs`] Register an exact `GET /healthz` handler before the catch-all SSR route. Return a constant non-sensitive HTTP 200 response without template reads, SSR, Apollo, GraphQL, media, or other dependency calls; preserve startup environment validation, static assets, redirects, error handling, and `/en` behavior. Refactor only as needed to make the regression seam deterministic and do not add a production dependency. (Requirement: all health-check scenarios; Design decision 1.)
- [x] 2.2 [Container artifact — `apps/ducth-dev-website/Dockerfile`] Retarget only the existing runner `HEALTHCHECK` URL to `http://localhost:3001/healthz`, preserving all other health-check options and runner settings. (Requirement: shallow and reader-compatibility scenarios; Design decision 2.)

## 3. Verification and operational handoff

- [x] 3.1 [Verification] Run `pnpm --dir apps/ducth-dev-website build`, the new focused production-server/Dockerfile regression tests, `pnpm --dir apps/ducth-dev-website test`, `pnpm --dir apps/ducth-dev-website typecheck`, `pnpm --dir apps/ducth-dev-website lint`, and a Docker image build when Docker is available. Review the diff and record any pre-existing unrelated failures separately. Confirm the `GET /healthz` test has no downstream GraphQL request and the existing `/en` tests still pass. (Requirements: all; Design verification.)
- [x] 3.2 [Release evidence prerequisite] Provide the Release Engineer with the image tag/digest, changed health-check command, focused test outputs, and planned post-rollout checks: container health becomes `healthy`, direct `/healthz` is HTTP 200, `/en` renders, and new recurring health-check-triggered `/posts/graphql/immutable` traces stop after old reader instances drain. State that this change needs no environment, schema, API, or API-service rollout; do not deploy without explicit authorization. Include rollback as redeploying the previous reader image (which restores `/en` probe traffic). (Requirement: all; Migration/rollback plan.)
