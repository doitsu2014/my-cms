## 1. Make workflow guidance accurate and safely classified

- [x] 1.1 Update `AGENTS.md` project-structure and layered-architecture sections to name `apps/api/gateway` as the composition root and `apps/api/domain_*` as the current owned implementation surfaces; retain retired `apps/api/src`, `apps/api/application_core`, and `apps/api/migration` references only when explicitly historical. **Affected artifact:** workflow documentation. **Prerequisite:** none. **Test-first:** not applicable—this is a documentation contract with no runtime behavior. **Verification:** run `rg -n 'apps/api/(src|application_core|migration)' AGENTS.md` and confirm every remaining match is explicitly historical (or remove all matches); run `rg -n 'apps/api/gateway|apps/api/domain_\*' AGENTS.md` and confirm both current paths are described.
- [x] 1.2 Add the all-conditions Fast Fix eligibility gate and normal-workflow escalation rule to the routing and Coder modes guidance in `AGENTS.md`: one non-generated file, at most 40 non-generated changed lines, no observable product/operator behavior change, no auth/API/schema/migration/dependency/deployment/secrets/generated-file/active-OpenSpec scope, and focused verification. **Affected artifact:** workflow documentation. **Prerequisite:** 1.1. **Test-first:** not applicable—this is a process contract. **Verification:** run `rg -n '40|one non-generated|active OpenSpec|normal.*OpenSpec|Fast Fix' AGENTS.md` and manually confirm all disqualifiers plus escalation are present.

## 2. Enable evidence-bound Product Designer discovery

- [x] 2.1 Add the existing project-local `code-review-graph` MCP server definition to `.codex/agents/product-designer.toml`, matching the command, args, and transport used by the existing SA/SE definitions. **Affected artifact:** Codex agent configuration. **Prerequisite:** none. **Test-first:** not applicable—this is declarative TOML configuration. **Verification:** run `python3 -c 'import tomllib; tomllib.load(open(".codex/agents/product-designer.toml", "rb"))'` and `rg -n '\[mcp_servers\.code-review-graph\]|code-review-graph.*serve' .codex/agents/product-designer.toml`; both commands must succeed and identify the expected server.
- [x] 2.2 Extend the Product Designer instructions in `.codex/agents/product-designer.toml` to require `get_minimal_context` first for cross-cutting UI/design-system work, constrain use to read-only graph discovery, forbid graph mutation/indexing/embeddings/wiki generation, and require graph evidence in the handoff. **Affected artifact:** Product Designer operating contract. **Prerequisite:** 2.1. **Test-first:** not applicable—this is an instruction contract. **Verification:** run `rg -n 'get_minimal_context|read-only|embedding|wiki|graph evidence' .codex/agents/product-designer.toml` and manually confirm the allowed and prohibited operations align with the delta spec.

## 3. Assign the release lifecycle to the Software Engineer

- [x] 3.1 Update `AGENTS.md` Phase 3, Phase 4, team contract, and quick reference to assign deployment readiness, rollout/rollback planning, post-deploy verification, and release handoff to the Software Engineer for operationally affected changes; require explicit user or release authorization before environment-changing deployment commands. **Affected artifact:** workflow documentation. **Prerequisite:** 1.1. **Test-first:** not applicable—this is a process ownership contract. **Verification:** run `rg -n 'deployment readiness|rollout|rollback|post-deploy|explicit.*authori' AGENTS.md` and confirm SE ownership and the authorization boundary are both stated.
- [x] 3.2 Update `.codex/agents/software-engineer.toml` startup, verification, and Phase 4 handoff instructions to perform the same operational readiness assessment and report either rollout/rollback evidence or that no deployment action is required. **Affected artifact:** Software Engineer operating contract. **Prerequisite:** 3.1. **Test-first:** not applicable—this is an instruction contract. **Verification:** run `rg -n 'deployment readiness|rollout|rollback|no deployment action|authorization' .codex/agents/software-engineer.toml` and confirm it neither grants autonomous production-deployment authority nor omits the non-operational case.

## 4. Increase exploration capacity with coordination controls

- [x] 4.1 Change `.codex/config.toml` to set `max_concurrent_threads_per_session = 4` and revise its comment to state that four available threads are for independent, read-only exploration/analysis while shared edits remain serialized. **Affected artifact:** project Codex configuration. **Prerequisite:** none. **Test-first:** not applicable—this is declarative TOML configuration. **Verification:** run `python3 -c 'import tomllib; c=tomllib.load(open(".codex/config.toml", "rb")); assert c["agents"]["max_concurrent_threads_per_session"] == 4'` and `git diff --check -- .codex/config.toml`; both commands must exit 0.
- [x] 4.2 Update `AGENTS.md` team-orchestration and Phase 1 guidance to permit up to four independent read-only workers only with `update_plan` checkpoints before dispatch and after synthesis, and to retain a single writer for all artifacts, configuration, and source files. **Affected artifact:** workflow documentation. **Prerequisite:** 4.1. **Test-first:** not applicable—this is a coordination contract. **Verification:** run `rg -n 'four|4|update_plan|read-only|single writer|serialize' AGENTS.md` and manually confirm the concurrency scope and both checkpoints are explicit.

## 5. Validate the governance change

- [x] 5.1 Review the complete diff against `proposal.md`, `specs/agent-workflow-governance/spec.md`, and `design.md`; resolve documentation/configuration drift and confirm no active change artifacts outside this change were edited. **Affected artifact:** change integration. **Prerequisite:** 1.1–4.2. **Test-first:** not applicable—this task validates declarative governance changes. **Verification:** run `git diff --check`, `git diff -- AGENTS.md .codex/config.toml .codex/agents/product-designer.toml .codex/agents/software-engineer.toml`, and `git status --short`; inspect and preserve unrelated worktree changes.
- [x] 5.2 Run the final OpenSpec readiness checks and record any warning before handing this change to the Software Engineer. **Affected artifact:** OpenSpec governance. **Prerequisite:** 5.1. **Test-first:** not applicable—OpenSpec validates the specification artifacts. **Verification:** run `openspec validate "harden-agent-workflow-quick-wins" --type change --strict --json` and `openspec status --change "harden-agent-workflow-quick-wins" --json`; validation must pass and all `applyRequires` artifacts must report `done`.
