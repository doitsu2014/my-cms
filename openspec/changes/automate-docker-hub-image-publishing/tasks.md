## 1. Update the publisher-only workflow contract

- [x] 1.1 [docker-hub-image-publishing] Revise `scripts/validate-app-image-build-contract.sh` first so it no longer requires `.github/workflows/validate-app-image-builds.yml` and instead asserts that the file is absent; it SHALL also assert the three app-scoped primary contexts, required frontend `editor-prose` named contexts, approved Docker Hub repository names, SHA/main-`latest` tagging policy, main-only publisher triggers, and absence of semantic-version tag triggers. Verify the check fails against a deliberately incorrect temporary workflow fixture and passes against the intended publisher-only configuration without Docker Hub authentication.
- [ ] 1.2 [docker-hub-image-publishing] Confirm GitHub repository secrets `DOCKER_USERNAME` and a scoped Docker Hub access token stored as `DOCKER_PASSWORD` are configured for the three approved public repositories; do not write credential values to tracked files. Record the configuration prerequisite and verify the local workflow-contract check runs without either secret.

## 2. Implement isolated publishing workflows

- [x] 2.1 [docker-hub-image-publishing] Update `.github/workflows/release-my-cms-image.yml` to publish only on relevant `main` pushes (`apps/api/**` plus its own workflow file) and intentional dispatches; remove semantic-version tag triggers. Preserve `apps/api` as the build context and `apps/api/Dockerfile` as the Dockerfile, publish only `doitsu2014/my-cms` with a SHA tag and `latest` only for main, and add least-privilege permissions plus API-specific latest-tag concurrency. Verify the focused workflow-contract check and a direct local Buildx API build.
- [x] 2.2 [docker-hub-image-publishing] Update `.github/workflows/release-my-cms-admin-image.yml` to publish only on relevant `main` pushes (`apps/web/**`, `packages/editor-prose/**`, and its own workflow file) and intentional dispatches; remove semantic-version tag triggers. Preserve `apps/web` plus `editor-prose=./packages/editor-prose`, publish only `doitsu2014/my-cms-admin` with SHA/main-`latest` tags, and add least-privilege permissions plus admin-specific latest-tag concurrency. Verify the focused workflow-contract check and a direct local Buildx admin build.
- [x] 2.3 [docker-hub-image-publishing] Add `.github/workflows/release-my-cms-ducth-dev-website-image.yml` using the established publisher pattern, but select only `apps/ducth-dev-website/**`, `packages/editor-prose/**`, and its own definition on `main` (plus intentional dispatch). Build from `apps/ducth-dev-website` with `editor-prose=./packages/editor-prose`; publish only `doitsu2014/my-cms-ducth-dev-website` with SHA/main-`latest` tags, least-privilege permissions, and Ducth-specific concurrency. Verify the focused contract check and a direct local Buildx Ducth build.

## 3. Remove CI validation and verify the publisher-only contract

- [x] 3.1 [docker-hub-image-publishing] Delete `.github/workflows/validate-app-image-builds.yml` and remove every tracked script or workflow reference that requires it. Verify `test ! -e .github/workflows/validate-app-image-builds.yml`, a repository search has no live reference other than this change's decision history, and no publisher declares a `pull_request` trigger.
- [x] 3.2 [docker-hub-image-publishing] Run `./scripts/validate-app-image-build-contract.sh` and YAML/action syntax review after the deletion. Confirm it checks the three publisher definitions without Docker Hub login, secrets, image pushes, or a dependency on the deleted workflow.
- [x] 3.3 [docker-hub-image-publishing] When Docker build inputs are changed, run direct local Buildx builds for API, admin, and Ducth plus `docker compose -f deployments/docker-swarm/apps/docker-compose.yaml config`. Confirm API uses only `apps/api`; each frontend receives the named `editor-prose` context; and no repository-root primary context is introduced.

## 4. Verify publishing behavior and handoff

- [ ] 4.1 [docker-hub-image-publishing] After repository-secret configuration, use controlled relevant `main` commits or intentional dispatches to verify each publisher. Confirm API-only selection does not publish frontend images; a shared `editor-prose` change publishes both frontend images but not API; and unrelated changes select no publisher. Record the generated SHA tag, digest, and `latest` target for each successful main publication.
- [ ] 4.2 [docker-hub-image-publishing] Verify publisher concurrency by starting or reviewing two consecutive relevant main runs for one image; confirm a superseded run cannot finish later and replace the newer run's `latest` tag. Preserve SHA/digest evidence for any successfully published revision.
- [x] 4.3 [docker-hub-image-publishing] Run the repository verification gate appropriate to the unchanged application source: `cargo check`, `cargo test`, `cargo fmt -- --check`, and `cargo clippy` from `apps/api`; `pnpm --dir apps/web test` and `pnpm --dir apps/web build`; and `pnpm --dir apps/ducth-dev-website test` and `pnpm --dir apps/ducth-dev-website build`. Record external Docker Hub/base-image/package-registry outages separately from application regressions.
- [ ] 4.4 [release handoff] Provide the Release Engineer with selected workflow runs, Docker Hub SHA tags and digests, credential-isolation evidence, local workflow-contract/direct-build results, and the rollback procedure (revert workflow configuration; if needed, authorized retag of the prior digest). State explicitly that no deployment, promotion, or environment mutation is authorized by this change.
