name: Build and Release my-cms Docker Image

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "apps/api/**"
      - ".github/workflows/release-my-cms-image.yml"

permissions:
  contents: read

concurrency:
  group: docker-hub-my-cms-api-publish
  cancel-in-progress: true

jobs:
  build_push_image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Docker Setup QEMU
        uses: docker/setup-qemu-action@v3.0.0
      - name: Docker Setup Buildx
        uses: docker/setup-buildx-action@v3.3.0
      - name: Docker Login
        uses: docker/login-action@v3.1.0
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          # list of Docker images to use as base name for tags
          images: |
            doitsu2014/my-cms
          # generate Docker tags based on the following events/attributes
          tags: |
            type=sha
            type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', 'main') }}
      - name: Build and push Docker images
        uses: docker/build-push-action@v5.3.0
        with:
          # Build's context is the set of files located in the specified PATH or URL
          context: "./apps/api"
          # Path to the Dockerfile
          file: "./apps/api/Dockerfile"
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=my-cms-api
          cache-to: type=gha,mode=max,scope=my-cms-api

# Disable auto deploy
# deploy:
#   needs: build_push_image
#   if: github.event_name != 'pull_request'
#   uses: ./.github/workflows/deploy-my-cms.yml
#   secrets: inherit
